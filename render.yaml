services:
  - type: web
    name: alemedu-demo
    runtime: docker
    plan: free
    autoDeployTrigger: off
    healthCheckPath: /api/health
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: AI_PROVIDER
        value: mock
      - key: APP_DEBUG
        value: "false"
